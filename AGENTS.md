# 全域 AGENTS.md

適用於所有學校備課、行政事務與自動化專案的通用規範。

---

## 🎯 核心原則：現況檔優先 (Live State First)

- 只要使用者曾修改任何 Excel／試算表／Google Sheets／文件，後續修正一律先讀取當下的現況檔，直接在該檔案上修改。
- 不得以舊版、範本或腳本重新產生結果後覆蓋現況檔；若必須重建，先保留現況檔建立副本，並確認使用者同意後才能替換。

---

## 🚀 新專案自動初始化規則

**觸發條件：** 工作目錄內沒有 `AGENTS.md`，且收到使用者第一則訊息時。

**動作流程：**
1. 主動詢問：「這個資料夾還沒有 AGENTS.md，這是新專案嗎？要建立標準檔案嗎？」
2. 使用者確認後，詢問：
   - 專案名稱（預設為資料夾名稱）
   - 類型（行政自動化 / GAS學習扶助 / GAS班級系統 / GAS教學工具 / GAS社群報告 / GAS錯題系統 / GAS生活工具 / 學科工具集 / 一般開發）
3. 同時建立以下五個標準檔案：
   - `CLAUDE.md`（含 YAML frontmatter + 技術框架 + 回答與資料來源規範 + 不能動的地方 + 部署後驗證清單）
   - `AGENTS.md`（精簡版：讀 CLAUDE.md + handoff.md + 遵守全域規則）
   - `CHANGELOG.md`（空白版本紀錄）
   - `PITFALLS.md`（含 ⚠️ 已知風險 結構）
   - `DECISIONS.md`（空白決策紀錄）

---

## 🛡️ 安全與開發規範

1. **修改程式碼前自動備份：** 修改現有程式碼檔案前，必須先將原始檔案內容備份為同目錄下的 `檔名.bak`。若 `.bak` 已存在則覆蓋。新建檔案不需備份。
2. **查詢與資料來源規則：** 所有查詢結果必須在回答結尾明確註明資料來源，凡檢索內容回答使用者時，必須在回答最後條列出參考的檔案名稱與 Markdown 超連結（含 `file://` 路徑）。
3. **文件優先原則：** 若使用者要求撰寫文件或遷移指南，必須先完成文件，再進行任何程式碼實作。

---

## 🔄 Session 結束流程

使用者說「結束」「收工」「要關了」時，依序執行：
1. 更新 `CLAUDE.md` frontmatter（status, version, next_action, updated）。
2. 更新或建立 `handoff.md`（已完成、目前進度、下一步、注意事項）。
3. 更新 `CHANGELOG.md`（若有完成功能或部署）。
4. 更新 `PITFALLS.md`（若有踩坑或修 bug）。
